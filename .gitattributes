# Normalise all text files to LF in the repo so Prettier's format:check
# (endOfLine: "lf") passes identically on Windows dev machines and Linux CI.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.woff binary
*.woff2 binary
*.ico binary
*.pdf binary
